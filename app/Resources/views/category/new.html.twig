{% extends 'layout.html.twig' %}

{% block body %}
    <h1>{{ 'category.creation' | trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'product.create' | trans }}" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('category_index') }}">{{ 'crud.back' | trans }}</a>
        </li>
    </ul>
{% endblock %}
