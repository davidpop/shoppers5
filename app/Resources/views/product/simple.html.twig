{% extends('layout.html.twig') %}

{% block body %}

<div class="bg-light py-3">
    <div class="container">
        <div class="row">
            <div class="col-md-12 mb-0"><a href="{{ path('accueil') }}">Accueil</a>
                <span class="mx-2 mb-0">/</span>
                <strong class="text-black">Panier</strong></div>
        </div>
    </div>
</div>

<div class="site-section">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h4>Vos Infos</h4>
                <dl>
                    <dt>Client</dt>
                    <dd>{{ app.user.nom | capitalize }}, {{ app.user.prenom | capitalize }}</dd>
                    <dt>Adresse complète</dt>
                    <dd>{{ app.user.adresse |capitalize }}, {{ app.user.codepostal }}</dd>
                </dl>
            </div>
        </div>
        <div class="row mb-5">
            {{ form(f) }}
        </div>
    </div>
</div>

{% endblock %}

